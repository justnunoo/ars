"use client";

import "./loading.css"; // Import the CSS file

export default function Loading() {
  return (
    <div className="loading-container">
      <div className="ars-logo">ARS</div>
      <div className="tagline">Precision, Protection, Progress.</div>
    </div>
  );
}
